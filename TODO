- CHECK type bubbling after changes to has- generation

- replace PascalCase and CamelCase by function names: e.g. GenerateClassName(Entity)

- Parser:
  - while parsing, check existence of identifier in identifier-expression (constr)
  - add header to generated files

- hpg: command line switches:
  - n(amespace) : generate namespace
  - i(nfo)      : suppress header in generated files
  - c(lass)=    : output class information (dot syntax)

- setup building of standalone hpg executable

- improve top-level error reporting
  - include entire stack of exceptions
  - interprete and format nicely
  - add position/line numbers, improve excerpt, cfr unit results

- don't generate interfaces for extractors
- collect "inline extractors", add to Extracting class

- reintroduce BNFSelfHostingTest

- redesign model ToString formatting to something more classic and format-able

- remove differences in EBNF notation
  - allow spaces in rule identifiers
  - change '::=' to '='
  - add comments

- create dynamic code emitter (-> interpreter from ENBF string)

- generate comments
  - rules in front of Parse<Entity> methods

- add license

- replace Make by xbuild

- check building in Windows environment
